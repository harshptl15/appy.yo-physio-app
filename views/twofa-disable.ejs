<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Disable Two‑Factor Authentication</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
  <link rel="stylesheet" href="/css/disable2fa.css">
</head>
<body>
  <div class="overlay">
    <form class="form-card animate__animated animate__fadeInDown" action="/twofa/disable" method="POST">
      <h1>Disable Two‑Factor Authentication</h1>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="error-msg"><%= error %></div>
      <% } %>

      <div class="helper-text">Confirm your password to disable 2FA.</div>
      <label for="password" class="sr-only">Password</label>
      <input
        type="password"
        id="password"
        name="password"
        class="form-control"
        placeholder="Password"
        autocomplete="current-password"
        required
        autofocus
      >

      <button type="submit">Disable 2FA</button>
      <a class="cancel-link" href="/dashboard">Cancel</a>
    </form>
  </div>
</body>
</html>
